
import PropTypes from "prop-types";

export default function TrustCard({ image, description }) {

    return (
        <>

        </>
    )
}

TrustCard.propTypes = {
    image: PropTypes.string.isRequired,
    description: PropTypes.string.isRequired,
}